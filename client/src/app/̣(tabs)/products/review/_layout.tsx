import React from 'react';
import { Stack } from 'expo-router';

const ReviewLayout = () => {
   return (
      <Stack
         screenOptions={{
            headerShown: false,
         }}
      ></Stack>
   );
};

export default ReviewLayout;
